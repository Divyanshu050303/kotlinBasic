fun main(args:Array<String>){
    var str:String="5" // Initialize  the number in the string format
    var num:Int=str.toInt() // we convert the string in to integer with help
    // ot toInt()Method
    print(num)
}